import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  circle: {
    flex: 1,
    paddingTop: 210,
    width: 400,
    height: 100,
    backgroundColor: "#526FD4",
    borderRadius: 100 / 2,
  },

  logoContainer: {
    flex: 1,
    width: 900,
    height: 400,
  },
});
